import { Write, WriteLine } from System.Console;
import Range from System.Linq.Enumerable;

func main() {
    for (i in Range(100)) {
        match (i) {
            (i mod 3 == 0) -> Write("Fizz");
            (i mod 5 == 0) -> Write("Buzz");
            else -> Write(i);
        }
        WriteLine();
    }
}
